{
  "name": "fitness-saas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "generate": "npm run generate -w database --if-present",
    "predev": "npm run generate -w database --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "devDependencies": {},
  "dependencies": {
    "lightningcss": "^1.32.0"
  },
  "optionalDependencies": {
    "lightningcss-win32-x64-msvc": "^1.32.0"
  }
}
